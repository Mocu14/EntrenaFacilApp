<?xml version="1.0" encoding="utf-8"?>
<!-- Layout para mostrar los detalles de una rutina -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Contenedor principal vertical -->
    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Carrusel de imágenes de la rutina (selección múltiple) -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPagerFotos"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:layout_marginBottom="8dp"
            android:clipToPadding="false"
            android:clipChildren="false"
            android:overScrollMode="never" />

        <!-- Indicadores del carrusel de imágenes -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayoutIndicadores"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="16dp" />

        <!-- Nombre de la rutina -->
        <TextView
            android:id="@+id/tvNombre"
            android:text="Nombre"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Descripción de la rutina -->
        <TextView
            android:id="@+id/tvDescripcion"
            android:text="Descripción"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp" />

        <!-- Tipo de rutina (Cardio, Fuerza...) -->
        <TextView
            android:id="@+id/tvTipo"
            android:text="Tipo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp" />

        <!-- Duración de la rutina -->
        <TextView
            android:id="@+id/tvDuracion"
            android:text="Duración"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp" />

        <!-- Día o días de la semana asignados -->
        <TextView
            android:id="@+id/tvDia"
            android:text="Día"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp" />

        <!-- Botón para editar la rutina -->
        <Button
            android:id="@+id/btnEditar"
            android:text="Editar Rutina"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"/>

        <!-- Botón para eliminar la rutina -->
        <Button
            android:id="@+id/btnEliminar"
            android:text="Eliminar Rutina"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"/>

        <!-- Botón para marcar la rutina como completada -->
        <Button
            android:id="@+id/btnCompletar"
            android:text="Marcar como Completada"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"/>

    </LinearLayout>
</ScrollView>
