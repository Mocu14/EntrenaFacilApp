<?xml version="1.0" encoding="utf-8"?>
<!-- Layout principal para mostrar y gestionar las rutinas del usuario -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Spinner que permite filtrar las rutinas según tipo o categoría -->
    <Spinner
        android:id="@+id/spinnerFiltro"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp" />

    <!-- Botón que permite al usuario crear una nueva rutina -->
    <Button
        android:id="@+id/btnAgregarRutina"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Crear nueva rutina"
        android:layout_marginBottom="12dp" />

    <!-- ListView que mostrará todas las rutinas existentes -->
    <ListView
        android:id="@+id/listViewRutinas"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:dividerHeight="8dp"/>
</LinearLayout>
